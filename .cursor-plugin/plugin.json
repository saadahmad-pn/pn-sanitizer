{
  "name": "pn-sanitizer",
  "description": "Paradigm Networks security scanner: gates prompts via beforeSubmitPrompt and file writes (Write + Edit) via preToolUse, scanning each against the CodeDefense API before allowing them through.",
  "version": "0.1.0",
  "author": {
    "name": "saadahmad-pn"
  },
  "repository": "https://github.com/saadahmad-pn/pn-sanitizer",
  "license": "MIT",
  "keywords": ["hooks", "security", "prompt-guard"],
  "hooks": "hooks/hooks.json",
  "variables": {
    "type": "object",
    "properties": {
      "PN_BASE_URL": {
        "type": "string",
        "title": "Paradigm Networks base URL",
        "description": "Your organization's PN URL, e.g. https://acme.paradigmnetworks.ai"
      },
      "PN_FAILURE_MODE": {
        "type": "string",
        "title": "If the scanner is unreachable",
        "description": "Block the action (recommended) or allow it through unscanned.",
        "enum": ["block", "allow"]
      }
    },
    "required": ["PN_BASE_URL"]
  }
}
